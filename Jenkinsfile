pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh './gradle clean build'
                echo 'successful build by bruce'
                echo 'successful build by bruce'
            }
        }
    }
}